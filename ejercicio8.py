def sumMayor(x):
        valor=[(conta**2) for conta in range(1,x+1)]        
        num=1
        while num<len(vacio):
                if valor[num]>100:
                        print valor[num]
                num+=1